export interface TodoList {
    id: string;
    title: string;
    content: string;
    createdAt: string;
    updatedAt: string;
}

export interface TodoData {
    title: string;
    content: string;
}
